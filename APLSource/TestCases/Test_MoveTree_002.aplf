 R←Test_MoveTree_002(stopFlag batchFlag);⎕TRAP;tempPath;more;list;success
⍝ Exercise `MoveTree` with an invalid right argument
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 tempPath←##.FilesAndDirs.GetTempPath,'***'

 :Trap 11
     (success more list)←∆Home ##.FilesAndDirs.MoveTree tempPath
     →T.FailsIf 1
 :Else
     →T.PassesIf'Right argument has wildcard characters'≡↑⎕DM
 :EndTrap

 R←T._OK
