 R←Test_CopyTree_004(stopFlag batchFlag);⎕TRAP
⍝ Exercise `CopyTree` with an invalid right argument
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :Trap 11
     ∆Home ##.FilesAndDirs.CopyTree 123
     →T.FailsIf 1
 :Else
     →T.PassesIf'Invalid right argument'≡⊃⎕DM
 :EndTrap

 R←T._OK
