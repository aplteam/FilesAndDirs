 R←Test_Dir2_014(stopFlag batchFlag);⎕TRAP;buff;type
⍝ Test the `Dir2` method on a directory without trailing slash and "type".
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 type←0 1 2 3 4 5 6 7
 buff←('type'type)##.FilesAndDirs.Dir2 ∆Home
 →T.PassesIf ∆Home≡1⊃buff
 →T.PassesIf(≢type)=≢buff

 R←T._OK
