 R←Test_CopyTree_001(stopFlag batchFlag);⎕TRAP
⍝ Exercise `CopyTree` with an invalid left argument
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :Trap 11
     ''##.FilesAndDirs.CopyTree''
     →T.FailsIf 1
 :Else
     →T.PassesIf'Left argument is not a directory'≡⊃⎕DM
 :EndTrap

 R←T._OK
