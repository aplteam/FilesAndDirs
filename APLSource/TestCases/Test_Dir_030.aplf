 R←Test_Dir_030(stopFlag batchFlag);⎕TRAP;list1;list2;path;list3
⍝ Test the `Dir` method recursively.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 path←∆Home
 list1←1⊃('recursive' 1)('follow' 0)##.FilesAndDirs.Dir path,'\'
 →T.PassesIf 20=≢list1     ⍝ Without following the symbolic link
 list2←1⊃('recursive' 1)##.FilesAndDirs.Dir path,'\'
 →T.PassesIf 23=≢list2

 R←T._OK
