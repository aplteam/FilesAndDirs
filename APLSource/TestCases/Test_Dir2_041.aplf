 R←Test_Dir2_041(stopFlag batchFlag);⎕TRAP;list
⍝ Test the `Dir2` method with wildcards matching directories
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 list←1⊃##.FilesAndDirs.Dir2'Tests\Test Dir\o*'
 →T.PassesIf 2=≢list
 →T.PassesIf∧/##.FilesAndDirs.IsDir list

 R←T._OK
