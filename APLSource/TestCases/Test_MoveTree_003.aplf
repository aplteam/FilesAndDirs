 R←Test_MoveTree_003(stopFlag batchFlag);⎕TRAP
⍝ Exercise `MoveTree` with an invalid left argument
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :Trap 11
     111 ##.FilesAndDirs.MoveTree''
     →T.FailsIf 1
 :Else
     →T.PassesIf'Invalid left argument'≡⊃⎕DM
 :EndTrap

 R←T._OK
