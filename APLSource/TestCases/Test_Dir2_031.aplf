 R←Test_Dir2_031(stopFlag batchFlag);⎕TRAP;list
⍝ Test the `Dir2` method recursively with a single wildcard at the end.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 list←1⊃('recurse' 1)##.FilesAndDirs.Dir2 ∆Home,'/*'
 →T.PassesIf 23=≢list

 R←T._OK
