 R←Test_ListDirs2_006(stopFlag batchFlag);list;list2;tempDir;⎕TRAP
⍝ Test the `ListDirs2` method on 'C:\'
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :If 'Lin'≡APLTreeUtils2.GetOperatingSystem ⍬
     tempDir←∆CreateSortFolder⊃⎕SI
     list←##.FilesAndDirs.ListDirs2 tempDir
     →T.GoToTidyUp∨/~##.FilesAndDirs.IsDir tempDir
     list←{2⊃⎕NPARTS ⍵}¨list
     →T.GoToTidyUp'AAA' 'aaa' 'BBB' 'bbb'≢list
     R←T._OK

∆TidyUp:
     {}##.FilesAndDirs.RmDirByForce tempDir

 :Else
     R←T._LinuxOnly
 :EndIf
