 r←Initial_CopyTree ps
⍝ Prevents the tests to be carried out in version 17.0 or better.
 :If ~r←~17<{⎕IO←⎕ML←1 ⋄ ⊃(//){⎕VFI ⍵/⍨2>+\'.'=⍵}2⊃⍵}# ⎕WG'APLVersion'
 :AndIf 0=ps.batchFlag
     ⎕←'"CopyTree" tests are suppressed because this can be achieved with built-in native function in this version of Dyalog'
 :EndIf
